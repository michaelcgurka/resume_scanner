# Optional: override API URL when running frontend separately.
# If unset, the app uses the same host as the page on port 8000 (e.g. http://127.0.0.1:8000 when opened at http://127.0.0.1:3000).
# REACT_APP_API_URL=http://localhost:8000
# REACT_APP_API_URL=http://127.0.0.1:8000
